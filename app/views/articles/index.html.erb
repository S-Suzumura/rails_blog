<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting!

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>
<% if account_signed_in? %>
  <p><%= link_to "New Article", new_article_path %></p>
  <p><%= button_to "Sign out", destroy_account_session_path,method: :delete,form: {data: {turbo_confirm: "Are you sure?"}} %></p>
<% else %>
  <p><%= link_to "Sign in", new_account_session_path %></p>
  <p><%= link_to "Create Account", new_account_registration_path %></p>
<% end %>